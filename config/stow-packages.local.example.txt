# Copy to config/stow-packages.local.txt to add personal machine-only stow packages.
# One package directory per line.
# Example local-only packages:
raycast
mcpm
vector
# Optional IDE config overlays:
vscode
cursor
